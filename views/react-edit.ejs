<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Note - SyncPad (React)</title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/react@18/umd/react.development.js"></script>
    <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        'sans': ['Inter', 'Helvetica', 'Arial', 'sans-serif'],
                    }
                }
            }
        }
    </script>
</head>

<body>
    <main class="w-full min-h-screen bg-zinc-100 font-['Helvetica']">
        <!-- Navigation -->
        <nav class="hidden md:flex px-10 justify-between items-center py-5">
            <div class="flex items-center gap-4">
                <a href="/" class="text-2xl tracking-tight logo">SyncPad</a>
                <span class="text-sm text-gray-500 font-light">Edit Note (React)</span>
            </div>
            <div class="navlinks flex gap-5 items-center">
                <a class="tracking-tight" href="/Home">← Back to Home</a>
                <a class="tracking-tight" href="/edit/<%= typeof noteId !== 'undefined' ? noteId : '' %>">EJS Version</a>
                <a class="tracking-tight px-4 py-2 bg-red-500 text-white rounded-md hover:bg-red-600 transition-colors" href="/logout">
                    Logout
                </a>
            </div>
        </nav>

        <!-- Mobile Navigation -->
        <nav class="md:hidden flex px-4 justify-between items-center py-4">
            <div class="flex items-center gap-4">
                <a href="/" class="text-xl tracking-tight logo">SyncPad</a>
                <span class="text-xs text-gray-500 font-light">Edit (React)</span>
            </div>
            <div class="flex gap-3 items-center">
                <a class="tracking-tight text-sm" href="/Home">← Back</a>
                <a class="tracking-tight text-sm" href="/edit/<%= typeof noteId !== 'undefined' ? noteId : '' %>">EJS</a>
            </div>
        </nav>
        
        <!-- React App Container -->
        <div id="react-app"></div>
        
        <!-- Footer -->
        <%- include('partials/footer') %>
    </main>

    <!-- Load React App -->
    <script src="/js/react-app.js"></script>
</body>
</html>
