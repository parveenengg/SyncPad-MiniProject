<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shared Note - SyncPad</title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <main class="w-full min-h-screen bg-zinc-100 font-['Helvetica']">
        <nav class="flex px-10 justify-between py-5">
            <div class="flex items-center gap-4">
                <a href="/" class="text-2xl tracking-tight logo">SyncPad</a>
                <span class="text-sm text-gray-500 font-light">Mini Project</span>
            </div>
            <div class="navlinks flex gap-5 items-center">
                <a class="tracking-tight" href="/">Home</a>
                <a class="tracking-tight" href="/login">Login</a>
            </div>
        </nav>
        
        <div class="px-10 notes">
            <div class="max-w-4xl mx-auto">
                <div class="bg-white rounded-lg shadow-md p-8 mt-8">
                    <!-- Note Header -->
                    <div class="flex items-center gap-3 mb-6">
                        <span class="px-3 py-1 bg-green-100 text-green-800 rounded-full flex items-center gap-2">
                            <i class="ri-share-line"></i>
                            Shared Note
                        </span>
                        <% if (note.encrypted) { %>
                            <span class="px-3 py-1 bg-blue-100 text-blue-800 rounded-full flex items-center gap-2">
                                <i class="ri-lock-line"></i>
                                Encrypted
                            </span>
                        <% } %>
                    </div>
                    
                    <!-- Note Title -->
                    <h1 class="text-3xl font-bold mb-6 text-gray-800"><%= note.title %></h1>
                    
                    <!-- Note Content -->
                    <div class="prose max-w-none mb-8">
                        <p class="text-lg text-gray-700 leading-relaxed whitespace-pre-wrap"><%= note.content %></p>
                    </div>
                    
                    <!-- Note Metadata -->
                    <div class="border-t border-gray-200 pt-6">
                        <p class="text-sm text-gray-500 mb-2">
                            <i class="ri-calendar-line mr-1"></i>
                            Created: <%= note.createdAt.toLocaleDateString() %> at <%= note.createdAt.toLocaleTimeString() %>
                        </p>
                        <% if (note.updatedAt && note.createdAt.getTime() !== note.updatedAt.getTime()) { %>
                            <p class="text-sm text-gray-500 mb-4">
                                <i class="ri-edit-line mr-1"></i>
                                Last Updated: <%= note.updatedAt.toLocaleDateString() %> at <%= note.updatedAt.toLocaleTimeString() %>
                            </p>
                        <% } %>
                        <p class="text-sm text-gray-500">
                            <i class="ri-share-line mr-1"></i>
                            This is a shared note from SyncPad
                        </p>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Footer -->
        <footer class="px-10 py-8 bg-gray-800 text-white">
            <div class="max-w-6xl mx-auto text-center">
                <p class="text-gray-400">© 2024 SyncPad Mini Project. Built with ❤️ for collaborative note-taking.</p>
            </div>
        </footer>
    </main>
</body>

</html>