<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard - SyncPad</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@3.5.0/fonts/remixicon.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        * {
            font-family: 'Inter', sans-serif;
        }
    </style>
</head>
<body class="bg-gray-50 min-h-screen">
    <!-- Admin Navigation -->
    <nav class="bg-white shadow-sm border-b border-gray-200">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-16">
                <!-- Logo and Title -->
                <div class="flex items-center">
                    <div class="w-8 h-8 bg-gradient-to-r from-red-500 to-orange-600 rounded-full flex items-center justify-center text-white text-sm font-bold mr-3">
                        <i class="ri-shield-user-line"></i>
                    </div>
                    <div>
                        <h1 class="text-xl font-bold text-gray-800">Admin Dashboard</h1>
                        <p class="text-sm text-gray-500">SyncPad Management</p>
                    </div>
                </div>
                
                <!-- Admin Info and Actions -->
                <div class="flex items-center space-x-4">
                    <div class="text-right">
                        <div class="text-sm font-medium text-gray-800"><%= admin.name %></div>
                        <div class="text-xs text-gray-500"><%= admin.role %></div>
                    </div>
                    <div class="flex items-center space-x-2">
                        <a href="/admin/users" class="px-3 py-2 text-gray-700 hover:text-blue-600 transition-colors">
                            <i class="ri-user-line mr-1"></i>
                            Users
                        </a>
                        <a href="/admin/logout" class="px-3 py-2 text-red-600 hover:text-red-700 transition-colors">
                            <i class="ri-logout-box-line mr-1"></i>
                            Logout
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <!-- Welcome Section -->
        <div class="mb-8">
            <h2 class="text-2xl font-bold text-gray-800 mb-2">System Overview</h2>
            <p class="text-gray-600">Monitor and manage your SyncPad platform</p>
        </div>

        <!-- Stats Grid -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
            <!-- Total Users -->
            <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
                <div class="flex items-center">
                    <div class="w-12 h-12 bg-blue-100 rounded-lg flex items-center justify-center">
                        <i class="ri-user-line text-blue-600 text-xl"></i>
                    </div>
                    <div class="ml-4">
                        <p class="text-sm font-medium text-gray-500">Total Users</p>
                        <p class="text-2xl font-bold text-gray-900"><%= stats.totalUsers %></p>
                    </div>
                </div>
            </div>

            <!-- Active Users -->
            <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
                <div class="flex items-center">
                    <div class="w-12 h-12 bg-green-100 rounded-lg flex items-center justify-center">
                        <i class="ri-user-heart-line text-green-600 text-xl"></i>
                    </div>
                    <div class="ml-4">
                        <p class="text-sm font-medium text-gray-500">Active Users</p>
                        <p class="text-2xl font-bold text-gray-900"><%= stats.activeUsers %></p>
                    </div>
                </div>
            </div>

            <!-- Total Notes -->
            <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
                <div class="flex items-center">
                    <div class="w-12 h-12 bg-purple-100 rounded-lg flex items-center justify-center">
                        <i class="ri-file-text-line text-purple-600 text-xl"></i>
                    </div>
                    <div class="ml-4">
                        <p class="text-sm font-medium text-gray-500">Total Notes</p>
                        <p class="text-2xl font-bold text-gray-900"><%= stats.totalNotes %></p>
                    </div>
                </div>
            </div>

            <!-- Storage Usage -->
            <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
                <div class="flex items-center">
                    <div class="w-12 h-12 bg-orange-100 rounded-lg flex items-center justify-center">
                        <i class="ri-database-2-line text-orange-600 text-xl"></i>
                    </div>
                    <div class="ml-4">
                        <p class="text-sm font-medium text-gray-500">Total Storage</p>
                        <p class="text-2xl font-bold text-gray-900"><%= stats.totalStorage %></p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Additional Stats -->
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
            <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
                <div class="flex items-center justify-between">
                    <div>
                        <p class="text-sm font-medium text-gray-500">Public Notes</p>
                        <p class="text-xl font-bold text-gray-900"><%= stats.publicNotes %></p>
                    </div>
                    <i class="ri-global-line text-blue-600 text-2xl"></i>
                </div>
            </div>

            <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
                <div class="flex items-center justify-between">
                    <div>
                        <p class="text-sm font-medium text-gray-500">Encrypted Notes</p>
                        <p class="text-xl font-bold text-gray-900"><%= stats.encryptedNotes %></p>
                    </div>
                    <i class="ri-lock-line text-green-600 text-2xl"></i>
                </div>
            </div>

            <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
                <div class="flex items-center justify-between">
                    <div>
                        <p class="text-sm font-medium text-gray-500">Admins</p>
                        <p class="text-xl font-bold text-gray-900"><%= stats.totalAdmins %></p>
                    </div>
                    <i class="ri-shield-user-line text-red-600 text-2xl"></i>
                </div>
            </div>
        </div>

        <!-- Top Storage Users -->
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
            <!-- Top Storage Users -->
            <div class="bg-white rounded-lg shadow-sm border border-gray-200">
                <div class="p-6 border-b border-gray-200">
                    <h3 class="text-lg font-semibold text-gray-800">Top Storage Users</h3>
                    <p class="text-sm text-gray-500">Users consuming the most storage</p>
                </div>
                <div class="p-6">
                    <% if (topStorageUsers.length > 0) { %>
                        <div class="space-y-4">
                            <% topStorageUsers.forEach((user, index) => { %>
                                <div class="flex items-center justify-between">
                                    <div class="flex items-center">
                                        <div class="w-8 h-8 bg-gradient-to-r from-blue-500 to-purple-600 rounded-full flex items-center justify-center text-white text-sm font-bold mr-3">
                                            <%= user.name.charAt(0).toUpperCase() %>
                                        </div>
                                        <div>
                                            <div class="font-medium text-gray-800"><%= user.name %></div>
                                            <div class="text-sm text-gray-500"><%= user.email %></div>
                                        </div>
                                    </div>
                                    <div class="text-right">
                                        <div class="font-medium text-gray-800"><%= user.formattedStorage %></div>
                                        <div class="text-sm text-gray-500"><%= user.notesCount %> notes</div>
                                    </div>
                                </div>
                            <% }); %>
                        </div>
                    <% } else { %>
                        <div class="text-center py-8 text-gray-500">
                            <i class="ri-database-2-line text-4xl mb-4 block"></i>
                            <p>No storage data available</p>
                        </div>
                    <% } %>
                </div>
            </div>

            <!-- Recent Users -->
            <div class="bg-white rounded-lg shadow-sm border border-gray-200">
                <div class="p-6 border-b border-gray-200">
                    <h3 class="text-lg font-semibold text-gray-800">Recent Users</h3>
                    <p class="text-sm text-gray-500">Latest user activity</p>
                </div>
                <div class="p-6">
                    <% if (recentUsers.length > 0) { %>
                        <div class="space-y-4">
                            <% recentUsers.forEach(user => { %>
                                <div class="flex items-center justify-between">
                                    <div class="flex items-center">
                                        <div class="w-8 h-8 bg-gradient-to-r from-blue-500 to-purple-600 rounded-full flex items-center justify-center text-white text-sm font-bold mr-3">
                                            <%= user.name.charAt(0).toUpperCase() %>
                                        </div>
                                        <div>
                                            <div class="font-medium text-gray-800"><%= user.name %></div>
                                            <div class="text-sm text-gray-500"><%= user.email %></div>
                                        </div>
                                    </div>
                                    <div class="text-right">
                                        <div class="flex items-center">
                                            <% if (user.isOnline) { %>
                                                <div class="w-2 h-2 bg-green-500 rounded-full mr-2"></div>
                                                <span class="text-sm text-green-600">Online</span>
                                            <% } else { %>
                                                <div class="w-2 h-2 bg-gray-400 rounded-full mr-2"></div>
                                                <span class="text-sm text-gray-500">Offline</span>
                                            <% } %>
                                        </div>
                                        <div class="text-xs text-gray-500">
                                            <%= new Date(user.lastLogin).toLocaleDateString() %>
                                        </div>
                                    </div>
                                </div>
                            <% }); %>
                        </div>
                    <% } else { %>
                        <div class="text-center py-8 text-gray-500">
                            <i class="ri-user-line text-4xl mb-4 block"></i>
                            <p>No recent users</p>
                        </div>
                    <% } %>
                </div>
            </div>
        </div>

        <!-- Quick Actions -->
        <div class="mt-8 bg-white rounded-lg shadow-sm border border-gray-200 p-6">
            <h3 class="text-lg font-semibold text-gray-800 mb-4">Quick Actions</h3>
            <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                <a href="/admin/users" class="flex items-center p-4 border border-gray-200 rounded-lg hover:bg-gray-50 transition-colors">
                    <i class="ri-user-settings-line text-blue-600 text-xl mr-3"></i>
                    <div>
                        <div class="font-medium text-gray-800">Manage Users</div>
                        <div class="text-sm text-gray-500">View and manage all users</div>
                    </div>
                </a>
                
                <a href="/admin/users?role=admin" class="flex items-center p-4 border border-gray-200 rounded-lg hover:bg-gray-50 transition-colors">
                    <i class="ri-shield-user-line text-red-600 text-xl mr-3"></i>
                    <div>
                        <div class="font-medium text-gray-800">Admin Users</div>
                        <div class="text-sm text-gray-500">Manage admin accounts</div>
                    </div>
                </a>
                
                <a href="/" class="flex items-center p-4 border border-gray-200 rounded-lg hover:bg-gray-50 transition-colors">
                    <i class="ri-external-link-line text-green-600 text-xl mr-3"></i>
                    <div>
                        <div class="font-medium text-gray-800">View Site</div>
                        <div class="text-sm text-gray-500">Go to main application</div>
                    </div>
                </a>
            </div>
        </div>
    </div>
</body>
</html>
